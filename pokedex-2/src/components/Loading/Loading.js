import pokeball from '../../assets/pokeball.png'
import { AnimatedPokeBall } from './styled'

const Loading = () => {
    return <AnimatedPokeBall src={pokeball} alt='loading' />
}

export default Loading